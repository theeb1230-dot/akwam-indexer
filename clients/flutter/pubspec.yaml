name: theeb_arab
description: ذيب العرب — Flutter mobile client.
publish_to: none
version: 0.2.5+8
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  theeb_client:
    path: ../dart
  url_launcher: ^6.3.1
  webview_flutter: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
