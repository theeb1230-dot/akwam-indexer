{
  "release_tag": "v0.2.5-experimental.1",
  "release_level": "experimental",
  "api_base_url": "https://theeb-arab-api.onrender.com",
  "flutter_version": "0.2.5+8",
  "artifacts": [
    "theeb-arab-android.apk",
    "theeb-arab-android-tv.apk",
    "theeb-arab-ios-unsigned.ipa"
  ],
  "note": "Retry v0.2.5 after pinning ANDROID_AVD_HOME for the installed-APK runtime smoke.",
  "retry": 5,
  "reason": "Release run #33 built all three artifacts, but emulator startup failed because the AVD was created outside the emulator search path. retry=5 pins ANDROID_AVD_HOME, verifies the .ini/.avd files, then reruns the same fail-closed Search UI smoke."
}
