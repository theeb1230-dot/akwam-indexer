# HTTP port
PORT=3000

# Runtime role.
# all: backward-compatible local runtime
# api: HTTP API runtime
THEEB_ROLE=all

# SQLite is local-development storage until the tested PostgreSQL migration.
# DATABASE_PATH=./data/theeb.sqlite
# DATABASE_URL=postgresql://...
# LOG_LEVEL=info
